Done. I've appended a comprehensive **Packages by Category** section to `research.md` covering:

- **14 categories** with full tables: Colorscheme, Editor/Core, Git, UI/Visual, Navigation, LSP, Completion, Treesitter, Formatting, Linting, DAP, Terminal, Utility Collections, AI Assistants, Notes
- **Extra Plugins** (`extraPlugins` — `opencode-nvim`)
- **Extra Packages** (all 14 system-level tools in `extraPackages` with their purpose)
- **System config** table (aliases, clipboard provider, allowUnfree)
- **Snacks sub-modules** table (all 10 configured features, noting it's currently disabled)
- **Formatters and linters** broken out per-tool with their target languages